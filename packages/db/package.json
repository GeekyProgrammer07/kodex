{
  "name": "@kodex/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --env-file=../../.env",
    "db:studio": "prisma studio --env-file=../../.env",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "prisma": "^6.0.0"
  },
  "devDependencies": {
    "@kodex/typescript-config": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "5.9.2"
  }
}